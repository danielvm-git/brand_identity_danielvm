# Architecture

## Current stack

- **Nuxt** (`nuxt.config.ts`) — projeto pode estar sem `package.json` até scaffold completo.
- **Brand tokens:** `brand/tokens.css`
- **Showcase:** `index.html`, estilos em `assets/css/`

## Decision snapshots

Opcional: manter `_LATEST.md` como ponteiro e `snapshots/YYYY-MM-DD-*.md` para decisões datadas quando o método maduro exigir auditoria.

## Pending decisions

*(ADR cortos ou bullets quando surgirem.)*
