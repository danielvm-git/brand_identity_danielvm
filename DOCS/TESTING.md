# Testing baseline

Até existir runner configurado (`package.json` + framework de testes):

- Preferir **testes automatizados** para lógica nova assim que o toolchain existir.
- **TDD por história** quando aplicável — ver `/speckit-story-execute`.
- Critérios de aceitação devem ser verificáveis (Given/When/Then ou checklist).

Atualizar esta página quando Vitest/Jest/playwright ou equivalente estiver definido.
